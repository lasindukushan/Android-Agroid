<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.splash">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PesticideCal"></activity>
        <activity android:name=".FindDay" />
        <activity android:name=".Plantshedule_u" />
        <activity android:name=".Dashboard" />
        <activity android:name=".DoChanges" />
        <activity android:name=".PSheduleMain" />
        <activity android:name=".market_admin" />
        <activity android:name=".admin_home" />
        <activity android:name=".admin_login" />
        <activity android:name=".district" />
        <activity android:name=".earn" />
        <activity android:name=".price" />
        <activity android:name=".signup" />
        <activity android:name=".login" />
        <activity android:name=".district_admin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>